# 운영자(플랫폼 슈퍼관리자) 페이지 설계서

> 스쿨디바이스 · 2026
> 대상 역할: **운영자(Operator)** — 전체 학교를 총괄하는 플랫폼 최고 권한

---

## 목차

1. [개요 및 역할 정의](#1-개요-및-역할-정의)
2. [현재 스키마 실사 결과](#2-현재-스키마-실사-결과)
3. [운영자 계정 설계](#3-운영자-계정-설계)
4. [대시보드 화면 구성](#4-대시보드-화면-구성)
5. [학교별 탭 사용 현황 기능](#5-학교별-탭-사용-현황-기능)
6. [SQL 구현](#6-sql-구현)
7. [구현 순서 및 주의사항](#7-구현-순서-및-주의사항)

---

## 1. 개요 및 역할 정의

### 1-1. 배경

현재 플랫폼은 **학교별 관리자(디지털정보부장) + 교사** 2단계 구조입니다. 학교 수가 늘어나면서 전체 학교를 조망하고 지원할 수 있는 상위 역할이 필요해졌습니다.

### 1-2. 역할 비교

| 구분 | 교사 | 관리자 | **운영자(신규)** |
|---|---|---|---|
| 인증 방식 | 학교코드 + 학급 선택 (localStorage) | Supabase Auth (이메일) | Supabase Auth (이메일) |
| 데이터 범위 | 본인 학급 | 소속 학교 전체 | **전체 학교** |
| 주 업무 | 기기 등록, 고장 신고, 대여 신청 | 학교 내 기기·대여·고장 처리 | **학교 계정 관리, 사용 현황 분석, 기술 지원** |
| 소속 학교 | 있음 | 있음 | **없음** |

### 1-3. 설계 원칙 — "내가 조치할 수 있는가"

운영자 대시보드에 넣을 지표는 **운영자가 직접 조치할 수 있는 것**으로 한정합니다.

- ❌ **처리 대기 중인 고장 신고 건수** — 각 학교 정보부장의 업무 큐. 운영자가 봐도 할 수 있는 조치가 없음
- ❌ **승인 대기 대여 건수** — 위와 동일
- ✅ **휴면 학교 수** — 연락해서 이탈 원인 파악 가능
- ✅ **가입 후 미설정 학교 수** — 온보딩 안내 발송 가능
- ✅ **아무도 안 쓰는 탭** — 기능 개선·제거 판단 가능

단, 고장 신고를 완전히 버리지는 않습니다. **개별 건이 아니라 집계 신호**로는 의미가 있습니다.
예: `30일 이상 미처리 고장 신고가 있는 학교` → 그 학교가 시스템을 방치하고 있다는 신호.
→ KPI 카드가 아니라 **학교 목록 테이블의 경고 아이콘**으로 표시.

---

## 2. 현재 스키마 실사 결과

`app/supabase-schema.sql`을 직접 확인한 결과입니다. 설계에 영향을 주는 항목이 몇 가지 있습니다.

### 2-1. ⚠️ RLS가 실질적으로 작동하지 않음

모든 테이블에 RLS가 켜져 있지만, 정책이 전부 무조건 허용입니다.

```sql
alter table classrooms enable row level security;
create policy "classrooms_all" on classrooms for all using (true) with check (true);
```

즉 **학교 간 데이터 격리가 DB가 아니라 애플리케이션 코드에서만 이뤄지고 있습니다.**

| 영향 | 내용 |
|---|---|
| 👍 운영자 기능 관점 | 별도 RLS 정책 수정 없이 운영자가 전체 데이터 조회 가능. 구현이 단순해짐 |
| 👎 보안 관점 | anon 키만 있으면 다른 학교 데이터도 조회 가능. **별도 과제로 반드시 개선 필요** |

> **권고**: 운영자 기능과 별개로, RLS 정책을 `school_id = current_school()` 형태로 조이는 작업을 백로그에 올려두세요. 운영자 예외는 `is_operator()` OR 조건으로 처리하면 됩니다. (6-4 참고)

### 2-2. `admin_profiles.school_id`는 UNIQUE — 소속 학교 없는 계정은 별도 테이블 필요

```sql
create table if not exists admin_profiles (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null unique references auth.users(id) on delete cascade,
  school_id uuid not null unique references school_config(id) on delete cascade,
  created_at timestamptz default now()
);
```

- `user_id`, `school_id` 모두 UNIQUE. **학교당 관리자 1명이 DB 레벨에서 보장됩니다.**
- **핵심**: `school_id`가 `NOT NULL`이므로 **소속 학교가 없는 운영자는 `admin_profiles`에 넣을 수 없습니다.**
  운영자용 별도 테이블(`operator_profiles`)이 필요한 이유가 바로 이것입니다 (3장 참고).

### 2-3. 시간 컬럼 이름이 테이블마다 다름

집계 쿼리 작성 시 반드시 확인해야 합니다.

| 테이블 | 시간 컬럼 | 비고 |
|---|---|---|
| `classrooms` | `created_at` | |
| `repair_reports` | **`reported_at`** | `created_at` 없음. `resolved_at` 별도 존재 |
| `rentals` | **`rented_at`** | `created_at` 없음. `returned_at` 별도 존재 |
| `shared_devices` | `created_at` | |
| `students` | `created_at` | |
| `chromebooks` | `created_at` | |
| `tutor_supports` | `created_at` | |
| `teacher_device_loans` | `created_at` | `updated_at`도 있음 |
| `classroom_devices` | **`updated_at` (nullable)** | `created_at` 없음 |

### 2-4. `devices`는 학교 공용 카탈로그

```sql
create table if not exists devices (
  id uuid primary key,
  device_type text not null unique,   -- 전역 유니크
  description text,
  created_at timestamptz default now()
);
```

- `school_id`가 **없습니다.** 모든 학교가 공유하는 기기 종류 마스터 테이블입니다.
- 따라서 "기기 관리" 탭의 학교별 활동은 `devices`가 아니라 **`classroom_devices` → `classrooms` 조인**으로 측정해야 합니다.
- `classroom_devices`에는 `school_id`도 `created_at`도 없으므로, `classrooms.school_id`로 조인하고 `updated_at`을 활동 시각으로 사용합니다.

---

## 3. 운영자 계정 설계

### 3-1. 별도 테이블 방식 채택

`admin_profiles`에 `role` 컬럼을 추가하는 방식은 `school_id NOT NULL` 제약과 충돌합니다. **별도 `operator_profiles` 테이블**을 만듭니다.

```sql
create table if not exists operator_profiles (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null unique references auth.users(id) on delete cascade,
  name text,
  created_at timestamptz default now()
);

alter table operator_profiles enable row level security;

-- 본인 레코드만 조회 가능 (권한 확인용)
create policy "operator_profiles_self_select" on operator_profiles
  for select using (auth.uid() = user_id);
```

**장점**

- 기존 `admin_profiles` 스키마를 건드리지 않음 → 기존 관리자 로직 무영향
- 소속 학교 없는 계정을 자연스럽게 표현
- 권한 판별이 `operator_profiles`에 행이 있는지 여부로 단순해짐

### 3-2. 계정 생성 경로

**회원가입 UI를 만들지 않습니다.** 운영자 계정은 다음 절차로만 생성합니다.

1. Supabase Dashboard → Authentication → Users에서 계정 수동 생성 (이미 있는 계정이면 생략)
2. SQL Editor에서 직접 INSERT — UUID를 직접 찾을 필요 없이 이메일로 바로 연결합니다.

```sql
insert into operator_profiles (user_id, name)
select id, '운영자' from auth.users where email = '<운영자 이메일>';
```

> INSERT/UPDATE/DELETE 정책을 만들지 않았으므로, service_role 키(Supabase Dashboard) 외에는 운영자 계정을 추가할 수 없습니다. 의도된 설계입니다.

**현재 운영자 계정**: `djsskach61@gmail.com` (기존 언남초등학교 관리자 계정과 동일). 아래처럼 이 계정으로 바로 등록합니다.

```sql
insert into operator_profiles (user_id, name)
select id, '운영자' from auth.users where email = 'djsskach61@gmail.com'
on conflict (user_id) do nothing;
```

> 이 계정은 `admin_profiles`에도 이미 연결되어 있어 언남초등학교 관리자 권한을 함께 가집니다.
> 3-3의 로그인 분기 로직상 `operator_profiles`를 먼저 확인하므로, 로그인 시 기본적으로
> `/operator/dashboard`로 이동합니다. 학교 관리자 화면이 필요하면 `/admin/dashboard`로 직접
> 이동하면 되며, `admin/layout.tsx`는 세션 유무만 검사하므로 접근에 문제가 없습니다.

### 3-3. 로그인 및 라우팅

기존 `/login/admin` 페이지를 **그대로 재사용**합니다. 별도 운영자 로그인 화면을 만들 필요가 없습니다.

로그인 성공 후 분기:

```
Supabase Auth 로그인 성공
   ↓
operator_profiles에서 user_id 조회
   ├─ 있음 → /operator/dashboard 로 이동
   └─ 없음 → admin_profiles 조회 → /admin/dashboard 로 이동
              (둘 다 없으면 → 학교 초기 설정 화면)
```

`app/src/app/operator/layout.tsx`에서 매 요청마다 `operator_profiles` 존재 여부를 확인하고, 없으면 `/login/admin`으로 리다이렉트합니다.

### 3-4. 라우트 구조

```
/operator
  ├── /dashboard            KPI 카드 + 탭 사용 히트맵
  ├── /schools              학교 목록 테이블
  ├── /schools/[id]         학교 상세 (읽기 전용)
  ├── /analytics            추이 차트
  ├── /notices              전체 공지 관리
  └── /logs                 감사 로그
```

---

## 4. 대시보드 화면 구성

### 4-1. KPI 카드 (상단)

"내가 조치할 수 있는가" 원칙에 따라 선정한 6개 지표입니다.

| 카드 | 정의 | 왜 필요한가 |
|---|---|---|
| **총 학교 수** | `school_config` 전체 행 수 | 서비스 규모 |
| **이번 주 신규 가입** | `created_at > now() - 7 days` | 성장 추이 |
| **활성 학교 수** | 최근 30일 내 어떤 탭이든 활동 있음 | 실사용 규모 |
| **휴면 학교 수** ⚠️ | 30일 이상 전 탭 무활동 | **연락 대상.** 이탈 징후 |
| **미설정 학교 수** ⚠️ | 가입했으나 `classrooms` 0건 | **온보딩 실패.** 가장 조치하기 쉬운 지표 |
| **총 등록 기기 수** | 크롬북 + 공유기기 + 학급기기 합계 | DB 용량 추정 |

⚠️ 표시 카드는 클릭 시 해당 학교 목록으로 필터링 이동합니다. **숫자만 보여주고 끝내면 의미가 없습니다.**

### 4-2. 학교 목록 테이블 (`/operator/schools`)

운영자 페이지에서 가장 많이 쓰이게 될 화면입니다.

| 학교명 | 학교코드 | 관리자 이메일 | 학급 수 | 기기 수 | 최근 활동 | 상태 |
|---|---|---|---|---|---|---|
| 언남초등학교 | UNNAM01 | admin@… | 24 | 312 | 2일 전 | 🟢 활발 |
| ○○초등학교 | OOELEM | admin@… | 0 | 0 | — | 🔴 미설정 |
| △△초등학교 | AAELEM | admin@… | 18 | 240 | 47일 전 | 🟡 휴면 ⚠️ |

**기능**

- 학교명·학교코드 검색, 컬럼 정렬
- 상태 필터 (전체 / 활발 / 저조 / 휴면 / 미설정)
- ⚠️ 아이콘 = 30일 이상 미처리 고장 신고 존재 (호버 시 건수 표시)
- 행 클릭 → `/operator/schools/[id]` 상세

**행 액션 (⋯ 메뉴)**

- 관리자 비밀번호 재설정 메일 발송
- 관리자 이메일 변경
- 학교코드 변경 (중복 검사 포함)
- 학교 비활성화 / 재활성화
- 학교 삭제 (2단계 확인 — `on delete cascade`로 전체 데이터가 함께 삭제되므로 학교명 직접 입력 요구)

### 4-3. 학교 상세 (`/operator/schools/[id]`)

**대리 로그인(impersonation)은 구현하지 않습니다.** 대신 **읽기 전용 조회 화면**을 제공합니다.

| 항목 | 대리 로그인 | 읽기 전용 조회 (채택) |
|---|---|---|
| 구현 난이도 | 높음 (세션 위조 필요) | 낮음 (school_id로 쿼리) |
| 사고 위험 | 운영자가 실수로 데이터 변경 가능 | 없음 |
| 문제 재현 | 완벽 | 데이터 확인은 충분 |

상세 화면에는 학교 기본정보, 탭별 사용 현황, 최근 활동 타임라인, 데이터 요약(학급/기기/대여/고장 건수)을 표시합니다.

### 4-4. 추이 차트 (`/operator/analytics`)

- 월별 신규 학교 가입 추이 (누적 + 신규)
- 학교별 등록 기기 수 상위 10개
- 전체 고장 신고 추이 및 평균 처리 소요 시간 (`resolved_at - reported_at`)
- 대여·반납 건수 월별 추이

### 4-5. 운영 기능

- **전체 공지**: 등록 시 모든 학교 관리자 대시보드 상단에 배너 노출. 기간 설정 가능
- **감사 로그**: 운영자가 어떤 학교 데이터를 언제 조회·변경했는지 기록. 개인정보(학생 정보) 접근 이력이 남아야 하므로 **필수**
- **시스템 현황**: Supabase DB 용량, 테이블별 행 수, 최근 오류 로그

---

## 5. 학교별 탭 사용 현황 기능

### 5-1. 접근 방식

**기존 업무 테이블의 레코드 생성 시각을 집계**합니다. 별도 로깅 테이블이나 추적 코드가 필요 없습니다.

| 방식 | 장점 | 단점 | 채택 |
|---|---|---|---|
| **데이터 기반 집계** | 추가 개발 0, 과거 데이터도 소급 적용 | 조회만 하는 사용은 미포착 | ✅ |
| 페이지 방문 로그 | 정확 | 새 테이블 필요, 빠르게 비대해짐 | ❌ (후순위) |

### 5-2. 탭 ↔ 테이블 매핑

실제 사이드바(`AdminSidebar.tsx`) 라우트 기준으로 정리했습니다. 학급 카드를 클릭하면 열리는
기기 수량 편집 모달(`classroom_devices`)은 별도 탭이 아니라 **학급 관리 탭 안의 기능**이므로
학급 관리와 같은 탭으로 집계합니다.

| 탭 | 라우트 | 집계 테이블 | 시간 컬럼 |
|---|---|---|---|
| 학급 관리 | `/admin/classrooms` | `classrooms` + `classroom_devices` ⋈ `classrooms` (기기 수량 편집 포함) | `created_at` / `updated_at` |
| 고장 신고 | `/admin/repairs` | `repair_reports` | `reported_at` |
| 공유 기기 관리 | `/admin/devices` | `shared_devices` | `created_at` |
| 대여 현황 | `/admin/rentals` | `rentals` | `rented_at` |
| 크롬북 관리 | `/admin/chromebooks` | `chromebooks` | `created_at` |
| 튜터 수업 지원 | `/admin/tutor` | `tutor_supports` | `created_at` |
| 교사기기대여 | `/admin/teacher-rentals` | `teacher_device_loans` | `created_at` |

> `students` 테이블은 크롬북 배정의 종속 데이터이므로 별도 탭으로 집계하지 않습니다.
>
> `/admin/device-types`(기기 종류 설정)는 `devices` 테이블을 다루는데, 이 테이블에는
> `school_id`가 없는 **전역 공용 카탈로그**입니다. 학교별 활동으로 나눌 수 없어 히트맵 집계에서
> 제외합니다.

### 5-3. 상태 분류

탭별로 세 값을 뽑아 4단계로 분류합니다.

- **총 레코드 수** → 도입 여부
- **최근 30일 생성 수** → 현재 활성도
- **마지막 활동 일시** → 이탈 시점

| 상태 | 조건 | 색상 |
|---|---|---|
| 🔴 미사용 | 총 0건 | 회색 |
| 🟠 초기설정만 | 데이터는 있으나 90일 이상 무활동 | 연회색 |
| 🟡 저조 | 최근 30일 1~4건 | 연한 파랑 |
| 🟢 활발 | 최근 30일 5건 이상 | 진한 파랑 |

> 기준 숫자(5건 / 30일 / 90일)는 실제 데이터를 본 뒤 조정합니다. 상수로 분리해두세요.

### 5-4. UI — 히트맵 매트릭스

**세로축 학교 × 가로축 탭** 형태가 정보 밀도가 가장 높습니다.

```
              학급   고장  공유  대여  크롬북  튜터  교사대여
언남초         ███    ██    ███   ███   ███     ██    ███
○○초          ▒▒▒    ░░    ▒▒    ░░    ███     ░░    ░░
△△초          ██     ░░    ░░    ░░    ░░      ░░    ░░
□□초           ░░    ░░    ░░    ░░    ░░      ░░    ░░
```

**읽는 법 — 이게 이 화면의 핵심 가치입니다**

| 패턴 | 해석 | 조치 |
|---|---|---|
| 특정 **열** 전체가 회색 | 그 기능을 아무도 안 씀 | 기능 개선 또는 제거 검토 |
| 특정 **행** 전체가 회색 | 온보딩 실패 또는 이탈 | 해당 학교 연락 |
| 다들 쓰는 탭을 한 학교만 안 씀 | 기능 존재를 모를 가능성 | 사용 안내 발송 |
| 특정 열만 전반적으로 연함 | 기능은 있으나 진입장벽 있음 | UX 점검 |

**인터랙션**

- 셀 호버 → `최근 30일 12건 · 마지막 활동 3일 전` 툴팁
- 셀 클릭 → 해당 학교 상세의 해당 탭으로 이동
- 열 헤더 클릭 → 그 탭 기준 정렬
- 학교가 20개 이상이면 세로 스크롤 + 헤더 고정

---

## 6. SQL 구현

### 6-1. 탭 사용 현황 뷰

`school_config`와 `admin_profiles`를 제외한 모든 업무 테이블을 `UNION ALL`로 합칩니다.
**2-3, 2-4에서 확인한 컬럼명 차이가 반영되어 있습니다.**

```sql
create or replace view school_tab_activity as
with events as (
  select school_id, '학급관리'::text as tab, created_at as at
    from classrooms

  union all
  -- classroom_devices(기기 수량 편집)는 /admin/classrooms 안의 기능이므로 같은 '학급관리' 탭으로 집계
  -- classroom_devices에는 school_id가 없어 classrooms로 조인
  select c.school_id, '학급관리', cd.updated_at
    from classroom_devices cd
    join classrooms c on c.id = cd.classroom_id

  union all
  select school_id, '고장신고', reported_at     from repair_reports
  union all
  select school_id, '공유기기', created_at      from shared_devices
  union all
  select school_id, '대여관리', rented_at       from rentals
  union all
  select school_id, '크롬북',   created_at      from chromebooks
  union all
  select school_id, '튜터지원', created_at      from tutor_supports
  union all
  select school_id, '교사대여', created_at      from teacher_device_loans
)
select
  school_id,
  tab,
  count(*)                                                        as total_count,
  count(*) filter (where at > now() - interval '30 days')         as recent_count,
  max(at)                                                         as last_activity,
  case
    when count(*) = 0                                       then '미사용'
    when max(at) < now() - interval '90 days'               then '초기설정만'
    when count(*) filter (where at > now() - interval '30 days') >= 5 then '활발'
    when count(*) filter (where at > now() - interval '30 days') >= 1 then '저조'
    else '초기설정만'
  end                                                             as status
from events
where at is not null          -- classroom_devices.updated_at은 nullable
group by school_id, tab;
```

> `classroom_devices.updated_at`이 nullable이라 `where at is not null`이 반드시 필요합니다. 이 조건이 없으면 `max(at)`가 NULL이 되어 상태 분류가 깨집니다.

### 6-2. 학교 요약 뷰

KPI 카드와 학교 목록 테이블이 함께 사용합니다.

```sql
create or replace view school_overview as
select
  s.id                                              as school_id,
  s.school_name,
  s.school_code,
  s.created_at                                      as joined_at,
  (select count(*) from classrooms c where c.school_id = s.id)   as classroom_count,
  (select count(*) from chromebooks cb where cb.school_id = s.id) as chromebook_count,
  (select coalesce(sum(total_quantity), 0)
     from shared_devices sd where sd.school_id = s.id)            as shared_device_count,
  (select max(last_activity) from school_tab_activity a
     where a.school_id = s.id)                                    as last_activity,
  (select count(*) from repair_reports r
     where r.school_id = s.id
       and r.status <> '처리 완료'
       and r.reported_at < now() - interval '30 days')            as stale_repair_count
from school_config s;
```

`stale_repair_count > 0`이면 학교 목록 테이블에 ⚠️ 아이콘을 표시합니다.

### 6-3. KPI 조회

```sql
select
  count(*)                                                            as total_schools,
  count(*) filter (where joined_at > now() - interval '7 days')       as new_this_week,
  count(*) filter (where last_activity > now() - interval '30 days')  as active_schools,
  count(*) filter (where last_activity is not null
                     and last_activity <= now() - interval '30 days') as dormant_schools,
  count(*) filter (where classroom_count = 0)                         as unconfigured_schools,
  sum(chromebook_count + shared_device_count)                         as total_devices
from school_overview;
```

### 6-4. 인덱스

학교 수가 늘면 `UNION ALL` 집계가 느려집니다. 각 테이블에 인덱스를 추가하세요.

```sql
create index if not exists idx_classrooms_school_created
  on classrooms (school_id, created_at);
create index if not exists idx_repair_school_reported
  on repair_reports (school_id, reported_at);
create index if not exists idx_rentals_school_rented
  on rentals (school_id, rented_at);
create index if not exists idx_shared_devices_school_created
  on shared_devices (school_id, created_at);
create index if not exists idx_chromebooks_school_created
  on chromebooks (school_id, created_at);
create index if not exists idx_tutor_school_created
  on tutor_supports (school_id, created_at);
create index if not exists idx_teacher_loans_school_created
  on teacher_device_loans (school_id, created_at);
create index if not exists idx_classroom_devices_classroom
  on classroom_devices (classroom_id, updated_at);
```

### 6-5. (참고) 향후 RLS 강화 시 운영자 예외 처리

2-1에서 지적한 RLS 문제를 나중에 개선할 때, 아래 헬퍼 함수를 만들어 모든 정책에 `or is_operator()`를 붙이면 운영자 전체 조회가 유지됩니다.

```sql
create or replace function is_operator()
returns boolean
language sql
security definer
stable
as $$
  select exists (
    select 1 from operator_profiles where user_id = auth.uid()
  );
$$;

-- 적용 예시
drop policy if exists "classrooms_all" on classrooms;
create policy "classrooms_read" on classrooms for select
  using (
    is_operator()
    or school_id in (
      select school_id from admin_profiles where user_id = auth.uid()
    )
  );
```

> 교사 로그인은 Supabase Auth를 쓰지 않고 localStorage 세션 기반이라 `auth.uid()`가 없습니다. RLS를 조일 때는 **교사용 접근 경로를 서버 라우트(service_role)로 옮기는 작업이 선행되어야 합니다.** 이 문서 범위를 벗어나는 별도 과제입니다.

---

## 7. 구현 순서 및 주의사항

### 7-1. 단계별 구현

| 단계 | 작업 | 산출물 |
|---|---|---|
| 1 | `operator_profiles` 테이블 생성 + 운영자 계정 수동 INSERT | SQL |
| 2 | 로그인 후 역할 분기 로직 추가 | `/login/admin` 수정 |
| 3 | `/operator` 레이아웃 + 권한 가드 | `operator/layout.tsx` |
| 4 | `school_tab_activity`, `school_overview` 뷰 생성 | SQL |
| 5 | KPI 카드 + 학교 목록 테이블 | `/operator/dashboard`, `/operator/schools` |
| 6 | 탭 사용 히트맵 | `/operator/dashboard` |
| 7 | 학교 상세 (읽기 전용) | `/operator/schools/[id]` |
| 8 | 감사 로그 | `/operator/logs` |
| 9 | 추이 차트, 전체 공지 | `/operator/analytics`, `/operator/notices` |

1~6단계까지가 최소 기능 범위(MVP)입니다.

### 7-2. 주의사항

**보안**

- 운영자 회원가입 UI를 **절대 만들지 마세요.** 계정 생성은 Supabase Dashboard 직접 접근으로만.
- 권한 확인은 클라이언트가 아니라 **서버(Route Handler 또는 Server Component)** 에서 수행합니다. 클라이언트 상태만 믿으면 우회 가능합니다.
- 학생 개인정보(`students` 테이블)가 포함되므로, 운영자의 학교 데이터 조회는 **반드시 감사 로그에 기록**합니다.

**데이터**

- 학교 삭제는 `on delete cascade`로 전체 데이터가 함께 삭제됩니다. 되돌릴 수 없으므로 학교명 직접 입력 확인 단계를 넣으세요.
- 학교코드 변경 시 교사들이 기존 코드로 로그인할 수 없게 됩니다. 변경 전 경고 표시 필요.

**설계**

- 상태 분류 기준값(30일 / 90일 / 5건)은 **상수로 분리**하세요. 실데이터를 본 뒤 반드시 조정하게 됩니다.
- 탭 사용 집계는 **쓰기 기준**입니다. 조회만 하는 사용은 미포착 — 대여 현황을 매일 확인만 하는 학교는 '미사용'으로 보일 수 있습니다. 학교 수가 적을 때는 이 정도로 충분하며, 정확도가 필요해지면 그때 `page_view_logs` 도입을 검토합니다.

### 7-3. 별도 백로그 (이 문서 범위 밖, 그러나 중요)

- **RLS 정책 실질화** — 현재 모든 정책이 `using (true)`. 학교 간 데이터 격리가 DB 레벨에서 이뤄지지 않고 있습니다.
