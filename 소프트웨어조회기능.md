# 소프트웨어 조회 탭 신설 기획서

| 항목      | 내용                                                                 |
| --------- | -------------------------------------------------------------------- |
| 문서명    | 학교 디지털기기 관리 플랫폼(스쿨디바이스) — 소프트웨어 조회 탭 신설 기획서 |
| 버전      | v1.0                                                                  |
| 작성일    | 2026년 8월 24일                                                       |
| 대상 독자 | 학교 정보 담당 교사(관리자), 일반 교사, 개발 담당자                   |
| 비고      | 별도 시스템이 아니라, 운영 중인 **스쿨디바이스** 플랫폼의 관리자·교사 대시보드에 새 탭을 추가하는 기획입니다 |

---

## 0. 기존 플랫폼과의 관계

이 플랫폼은 지금까지 **하드웨어**(크롬북·태블릿·빔프로젝터 등)를 다뤄 왔습니다.
이번에 신설하는 **`소프트웨어 조회`** 탭은 플랫폼에서 처음으로 **소프트웨어**를 다룹니다.

| 구분        | 기존 탭들 (`공유 기기`·`크롬북 관리`·`교사기기대여`) | 신설 `소프트웨어 조회` 탭            |
| ----------- | ------------------------------------------------------ | -------------------------------------- |
| 대상        | 물리적 기기                                            | 학교에서 쓰는 소프트웨어·웹서비스     |
| 관리자 역할 | 기기 등록·대여 승인                                    | 심의받은 소프트웨어 목록 관리, 신청 심의 |
| 교사 역할   | 기기 신청·고장 신고                                    | 심의 목록 조회, 신규 소프트웨어 신청  |
| 산출물      | 대여증·반납증                                          | 심의 목록(조회용), 신청 처리 상태     |

다만 아래 기존 자산은 그대로 재사용합니다.

- **엑셀 일괄 등록** — `크롬북 관리` 탭의 업로드 방식(`xlsx` 동적 import, 열 제목 유연 매칭,
  미리보기 후 확정)을 그대로 따릅니다.
- **학교 분리** — `school_id` 컬럼 + 애플리케이션 계층 필터링(`useSchool` 훅).
- **감사 로그** — 관리자의 등록·수정·상태 변경은 기존 `logAudit()`으로 `audit_logs`에 남깁니다.
- **공통 컴포넌트** — `Button`·`Modal`·`Badge`.

교사 사이드바에는 `소프트웨어 조회`, 관리자 사이드바에는 같은 이름의 탭을 두되
관리자 화면에만 등록·심의 기능이 나타납니다.

---

## 1. 개요

### 1.1 배경

학교에서 교사가 수업에 쓸 소프트웨어를 고를 때 두 가지 어려움이 있습니다.

- **무엇을 써도 되는지 모릅니다.** 개인정보 보호와 예산 문제로 학교마다 심의를 거쳐
  사용 가능한 소프트웨어를 정해 두지만, 그 목록이 공문·메신저·인쇄물에 흩어져 있어
  교사가 필요할 때 찾아보기 어렵습니다. 결국 담당 교사에게 매번 물어보게 됩니다.
- **신청 절차가 보이지 않습니다.** 새 소프트웨어를 쓰고 싶으면 담당 교사에게 말로
  요청하고, 그 뒤 어떻게 되고 있는지는 다시 물어봐야 알 수 있습니다. 담당 교사도
  누가 무엇을 신청했는지 메모나 기억에 의존합니다.

### 1.2 목적

**심의받은 소프트웨어 목록을 교사가 언제든 스스로 찾아볼 수 있게 하고**,
**새 소프트웨어 신청과 그 처리 상태를 한 화면에서 주고받게 합니다.**

관리자는 이미 갖고 있는 심의 목록(대개 엑셀 파일)을 그대로 올리기만 하면 되고,
교사는 검색과 색인으로 목록을 훑고 필요한 것을 신청합니다.

### 1.3 범위

**포함**

- 관리자·교사 대시보드에 `소프트웨어 조회` 탭 신설 (탭 내부는 두 화면으로 구분)
  - ① 심의받은 소프트웨어 목록 조회
  - ② 신청한 소프트웨어 조회
- 관리자: 엑셀 일괄 등록, 개별 추가, 수정, 사용중지
- 교사: 심의 목록 조회 + 검색·색인, 신규 소프트웨어 신청
- 관리자: 신청 건의 상태 변경 (`처리중` → `심의완료`)

**제외** (1차 범위 밖)

- 심의 절차 자체의 전자결재(품의·위원회 의결 기록)
- 라이선스 수량·계약 기간·예산 관리
- 계정 발급, 로그인 연동(SSO), 사용량 통계
- 에듀집·에듀넷 등 외부 시스템과의 실시간 API 연계 (URL 링크 보관까지만)
- 학생 대상 화면

---

## 2. 사용자

### 2.1 사용자 구분

| 구분                    | 로그인 방식                          | 이 탭에서 하는 일                              |
| ----------------------- | ------------------------------------- | ----------------------------------------------- |
| 학교 관리자 (정보 담당) | 기존 Supabase Auth 관리자 계정        | 심의 목록 등록·수정, 신청 건 심의 상태 변경    |
| 교사                    | 기존 학급 선택 로그인(`/login/teacher`) | 심의 목록 조회·검색, 신규 소프트웨어 신청       |

이 플랫폼의 교사 로그인은 **학급 단위**입니다(개인 계정이 없습니다).
따라서 신청 기록에는 `classroom_id`와 함께 **신청 교사 이름을 직접 입력**받아 남깁니다.
누가 신청했는지 관리자가 확인해야 하고, 학급 담임이 아닌 교과 교사가 학급 계정으로
접속하는 경우도 있기 때문입니다.

### 2.2 사용 시나리오

**시나리오 A — 관리자가 심의 목록을 올립니다**

> 3월 초, 정보 담당 교사가 작년 심의 결과 엑셀 파일을 갖고 있습니다.
> `소프트웨어 조회` 탭 → `엑셀로 추가`를 눌러 파일을 고릅니다.
> 화면에 읽어들인 목록이 표로 미리보기 됩니다. 이미 등록된 것은 `갱신`,
> 처음 보는 것은 `신규`로 표시됩니다. `확인`을 누르면 한 번에 등록됩니다.

**시나리오 B — 교사가 찾아봅니다**

> 4학년 담임 교사가 수업에 쓸 그림 도구를 찾습니다.
> `소프트웨어 조회` 탭에서 색인 `ㅁ`을 누르거나 검색창에 `미리`를 칩니다.
> 목록에서 `미리캔버스`를 찾고, 에듀집 등록 여부와 주소 링크를 확인합니다.
> 링크를 눌러 바로 접속합니다.

**시나리오 C — 교사가 신청합니다**

> 찾는 소프트웨어가 목록에 없습니다.
> `소프트웨어 신청`을 눌러 이름·회사·에듀집 등록 여부·주소·사용 목적을 적고 보냅니다.
> 신청 직후 `신청한 소프트웨어` 화면에 `처리중` 상태로 나타납니다.

**시나리오 D — 관리자가 심의합니다**

> 관리자가 `신청 현황`에서 `처리중` 3건을 봅니다.
> 위원회 심의를 거친 뒤 각 건을 `심의완료`로 바꿉니다.
> 이때 `심의 목록에도 추가` 체크가 켜져 있어, 해당 소프트웨어가 곧바로
> 심의받은 목록에 들어가 모든 교사에게 보입니다.

---

## 3. 기능 요구사항

우선순위 — **필수**: 1차 개발 대상 / **선택**: 여력이 되면 / **추후**: 2차 이후

| ID   | 기능                    | 대상   | 설명                                                                     | 우선순위 |
| ---- | ----------------------- | ------ | ------------------------------------------------------------------------- | -------- |
| S-01 | 심의 목록 엑셀 일괄 등록 | 관리자 | 엑셀·CSV 파일을 읽어 미리보기 후 일괄 등록. 기존 항목은 갱신              | 필수     |
| S-02 | 심의 목록 개별 추가     | 관리자 | 모달에서 한 건씩 직접 입력                                                | 필수     |
| S-03 | 심의 목록 수정          | 관리자 | 등록된 항목의 내용을 고침                                                 | 필수     |
| S-04 | 심의 목록 사용중지      | 관리자 | 더 이상 쓰지 않는 항목을 목록에서 숨김(삭제가 아닌 `is_active = false`)   | 필수     |
| S-05 | 심의 목록 조회          | 공통   | 이름·회사·에듀집 등록 여부·주소를 표로 표시                               | 필수     |
| S-06 | 검색                    | 공통   | 소프트웨어명·회사명 부분 일치 검색                                        | 필수     |
| S-07 | 색인                    | 공통   | 첫 글자(한글 초성 / 영문 / 숫자·기호) 색인 바로 건너뛰기                  | 필수     |
| S-08 | 필터·정렬               | 공통   | 에듀집 등록 여부 필터, 가나다순·최근 등록순 정렬                          | 필수     |
| S-09 | 소프트웨어 신청         | 교사   | 이름·회사·에듀집 등록 여부·주소·신청 교사명·사용 목적 입력                | 필수     |
| S-10 | 신청 목록 조회          | 공통   | 교사는 우리 학급 신청(전체 보기 전환 가능), 관리자는 학교 전체 신청 조회  | 필수     |
| S-11 | 신청 상태 변경          | 관리자 | `처리중` ↔ `심의완료`                                                     | 필수     |
| S-12 | 심의완료 → 목록 편입    | 관리자 | `심의완료` 처리 시 심의 목록에 자동 등록(체크박스로 끌 수 있음)           | 필수     |
| S-13 | 중복 신청 안내          | 교사   | 신청 입력 중 이름이 심의 목록·기존 신청과 겹치면 화면에서 알림            | 필수     |
| S-14 | 학교별 데이터 분리      | 공통   | 기존과 동일하게 `school_id`로 분리                                        | 필수     |
| S-15 | 감사 로그               | 관리자 | 등록·수정·상태 변경을 `audit_logs`에 기록                                 | 필수     |
| S-16 | 엑셀 양식 다운로드      | 관리자 | 업로드용 빈 양식(열 제목 포함) 내려받기                                   | 선택     |
| S-17 | 심의 목록 내보내기      | 관리자 | 현재 목록을 엑셀로 저장(백업·공문 첨부용)                                 | 선택     |
| S-18 | 관리자 메모·반려        | 관리자 | 신청 건에 처리 사유를 남기고 `반려` 상태 추가                             | 추후     |
| S-19 | 교사 알림               | 교사   | 신청 건이 심의완료되면 대시보드에 표시                                    | 추후     |

### 3.1 입력 항목 명세

**심의받은 소프트웨어 (관리자 입력)**

| 항목             | 형식      | 필수 | 비고                                              |
| ---------------- | --------- | ---- | -------------------------------------------------- |
| 소프트웨어명     | 텍스트    | ○    | 목록의 기준이 되는 이름                            |
| 회사(제작사)     | 텍스트    |      | 동명 소프트웨어 구분에 사용                        |
| 에듀집 등록 여부 | 예 / 아니오 | ○  | 기본값 `아니오`                                    |
| 주소(URL)        | 텍스트    |      | `http(s)://`로 시작하지 않으면 저장 시 `https://` 보정 |
| 비고             | 긴 텍스트 |      | 사용 조건, 학년 제한 등                            |

**소프트웨어 신청 (교사 입력)**

| 항목             | 형식      | 필수 | 비고                                    |
| ---------------- | --------- | ---- | ---------------------------------------- |
| 소프트웨어명     | 텍스트    | ○    |                                          |
| 회사(제작사)     | 텍스트    | ○    | 심의 확인에 필요하므로 신청 시에는 필수  |
| 에듀집 등록 여부 | 예 / 아니오 | ○  | 교사가 아는 대로 표시. 관리자가 최종 확인 |
| 주소(URL)        | 텍스트    | ○    | 심의 대상을 특정하기 위해 필수           |
| 신청 교사명      | 텍스트    | ○    | 학급 계정을 공용으로 쓰므로 이름을 받음  |
| 사용 목적        | 긴 텍스트 |      | 어떤 수업에 어떻게 쓸지                  |

> **용어 확인 필요** — `에듀집`은 신청·심의 판단의 근거가 되는 외부 등록처를 가리키는
> 현장 용어로 이해하고 그대로 표기했습니다. 정식 명칭·주소 형식이 정해져 있다면
> 라벨 문구만 바꾸면 됩니다(12장 참고).

---

## 4. 화면 설계

### 4.1 화면 목록

| 화면                    | 경로                 | 접근   | 설명                                     |
| ----------------------- | -------------------- | ------ | ----------------------------------------- |
| 관리자 — 소프트웨어 조회 | `/admin/software`    | 관리자 | 두 개의 안쪽 탭(심의 목록 / 신청 현황)   |
| 교사 — 소프트웨어 조회   | `/teacher/software`  | 교사   | 두 개의 안쪽 탭(심의 목록 / 내 신청)     |

사이드바 라벨은 양쪽 모두 `소프트웨어 조회`(아이콘 `🧩`)로 통일하고,
관리자 사이드바에서는 `크롬북 관리` 아래에 둡니다.

### 4.2 화면 흐름

```
[관리자]  소프트웨어 조회
            ├─ 심의 목록 탭 ─┬─ [엑셀로 추가] → 파일 선택 → 미리보기 표 → 확인 → 등록
            │                ├─ [개별 추가]   → 입력 모달 → 저장
            │                ├─ 행 클릭       → 수정 모달 → 저장 / 사용중지
            │                └─ [양식 다운로드] · [내보내기]
            └─ 신청 현황 탭 ─── 신청 카드 → [심의완료로 변경] → (심의 목록에도 추가 ☑) → 저장

[교사]    소프트웨어 조회
            ├─ 심의 목록 탭 ─┬─ 검색창 / 색인 바 / 에듀집 필터 / 정렬
            │                └─ 주소 링크 클릭 → 새 창
            └─ 내 신청 탭  ─┬─ [소프트웨어 신청] → 입력 모달 → (중복 안내) → 보내기
                             └─ 상태 배지 확인 (처리중 / 심의완료)
```

### 4.3 심의 목록 화면 구성

상단에 검색·필터, 그 아래 색인 바, 그 아래 목록입니다.

```
┌──────────────────────────────────────────────────────────────┐
│ 🔍 [ 소프트웨어명·회사명 검색            ]  [에듀집 등록만 ▢] │
│ 정렬: (가나다순) 최근 등록순          총 128건 / 표시 12건   │
├──────────────────────────────────────────────────────────────┤
│ 전체 ㄱ ㄴ ㄷ ㄹ ㅁ ㅂ ㅅ ㅇ ㅈ ㅊ ㅋ ㅌ ㅍ ㅎ | A B C … Z | # │
├──────────────────────────────────────────────────────────────┤
│ 소프트웨어명      회사        에듀집   주소          (관리자) │
│ ────────────────────────────────────────────────────────────  │
│ 미리캔버스        미리캔버스   등록    바로가기 ↗    수정      │
│ 매쓰홀릭          매쓰홀릭     미등록  바로가기 ↗    수정      │
└──────────────────────────────────────────────────────────────┘
```

- 마지막 `수정` 열은 **관리자 화면에서만** 나타납니다. 교사 화면에는 없습니다.
- 에듀집 열은 `Badge`로 표시합니다 — 등록(초록) / 미등록(회색).
- 주소는 `target="_blank" rel="noopener noreferrer"` 새 창 링크입니다.
- 모바일·태블릿 폭에서는 표 대신 카드 목록으로 전환합니다.

### 4.4 색인(S-07) 동작 규칙

색인은 **소프트웨어명의 첫 글자**를 기준으로 그룹을 만듭니다.

| 첫 글자 | 그룹                                                          |
| ------- | -------------------------------------------------------------- |
| 한글    | 초성 14자(ㄱ ㄴ ㄷ ㄹ ㅁ ㅂ ㅅ ㅇ ㅈ ㅊ ㅋ ㅌ ㅍ ㅎ). 쌍자음은 대응하는 기본 자음에 합침(ㄲ→ㄱ, ㄸ→ㄷ, ㅃ→ㅂ, ㅆ→ㅅ, ㅉ→ㅈ) |
| 영문    | A–Z (대소문자 구분 없음)                                       |
| 그 외   | `#` (숫자·기호로 시작하는 이름)                                |

한글 초성은 유니코드 계산으로 뽑습니다. 별도 라이브러리를 쓰지 않습니다.

```ts
const CHO = ['ㄱ','ㄲ','ㄴ','ㄷ','ㄸ','ㄹ','ㅁ','ㅂ','ㅃ','ㅅ',
             'ㅆ','ㅇ','ㅈ','ㅉ','ㅊ','ㅋ','ㅌ','ㅍ','ㅎ']
const FOLD: Record<string, string> = { 'ㄲ':'ㄱ', 'ㄸ':'ㄷ', 'ㅃ':'ㅂ', 'ㅆ':'ㅅ', 'ㅉ':'ㅈ' }

function indexKey(name: string): string {
  const ch = name.trim()[0] ?? '#'
  const code = ch.charCodeAt(0)
  if (code >= 0xac00 && code <= 0xd7a3) {
    const cho = CHO[Math.floor((code - 0xac00) / 588)]
    return FOLD[cho] ?? cho
  }
  if (/[a-zA-Z]/.test(ch)) return ch.toUpperCase()
  return '#'
}
```

- 색인 글자를 누르면 해당 그룹만 남습니다. 다시 누르거나 `전체`를 누르면 해제됩니다.
- **해당 글자로 시작하는 항목이 하나도 없으면 그 글자는 흐리게 표시하고 누를 수 없게** 합니다.
- 색인과 검색어는 **함께 적용**됩니다(AND).
- 정렬 기본값은 `localeCompare(name, 'ko')` 가나다순입니다.

목록 규모(학교당 수십~수백 건)를 감안해 **전체를 한 번 불러와 클라이언트에서
필터링**합니다. 검색어를 칠 때마다 서버에 묻지 않으므로 반응이 즉각적입니다.

### 4.5 신청 화면 구성

**교사 — 내 신청**

```
┌──────────────────────────────────────────────────┐
│ [ + 소프트웨어 신청 ]      ▢ 학교 전체 신청 보기 │
├──────────────────────────────────────────────────┤
│ 미리캔버스 · 미리캔버스        [처리중]          │
│ 에듀집 등록 · https://...                        │
│ 신청 김하늘 · 3학년 1반 · 8월 20일               │
├──────────────────────────────────────────────────┤
│ 캔바 · Canva                   [심의완료]        │
│ …                                                 │
└──────────────────────────────────────────────────┘
```

`학교 전체 신청 보기`를 켜면 다른 학급이 이미 신청한 건도 보입니다.
같은 소프트웨어를 여러 교사가 중복 신청하는 것을 줄이기 위한 장치입니다.

**관리자 — 신청 현황**

```
┌──────────────────────────────────────────────────────────┐
│ 상태: (전체) 처리중 심의완료      🔍 [ 검색 ]            │
├──────────────────────────────────────────────────────────┤
│ 미리캔버스 · 미리캔버스   에듀집 등록   [처리중 ▾]        │
│ https://www.miricanvas.com  ↗                            │
│ 신청 김하늘 · 3학년 1반 · 8월 20일                        │
│ 사용 목적: 4학년 미술 포스터 제작 수업에 사용            │
└──────────────────────────────────────────────────────────┘
```

상태 배지를 누르면 상태 변경 모달이 열립니다.

**상태 변경 모달 (`처리중` → `심의완료`)**

```
심의완료로 변경하시겠습니까?

  ☑ 심의받은 소프트웨어 목록에도 추가
     ─ 소프트웨어명 [ 미리캔버스        ]
     ─ 회사         [ 미리캔버스        ]
     ─ 에듀집 등록  (●) 등록  ( ) 미등록
     ─ 주소         [ https://...       ]

               [ 취소 ]  [ 변경 ]
```

체크를 켠 채로 변경하면 신청 내용이 심의 목록에 그대로 들어갑니다(S-12).
관리자가 이름·회사를 다듬을 수 있도록 편집 가능한 입력란으로 보여 줍니다.
이미 같은 이름·회사가 목록에 있으면 새로 만들지 않고 **기존 항목에 연결**합니다.

### 4.6 엑셀 업로드 미리보기

`크롬북 관리` 탭과 동일한 흐름입니다.

```
읽어들인 목록 24건 (신규 18 · 갱신 6)

  상태   소프트웨어명   회사        에듀집   주소
  신규   미리캔버스     미리캔버스   등록     https://...
  갱신   패들렛         Padlet      미등록   https://...
  …

  ⚠ 이름이 비어 있는 3개 행은 제외됩니다.

                       [ 취소 ]  [ 확인하고 등록 ]
```

---

## 5. 데이터 설계

### 5.1 개체 관계

```
  school_config ──1:N── approved_software      (심의받은 소프트웨어 목록)
        │                       ▲
        │                       │ 0..1  approved_software_id
        └────1:N──────── software_requests      (교사가 신청한 소프트웨어)
                                │
  classrooms ──────1:N──────────┘  classroom_id
```

두 테이블을 나눈 이유는 **성격이 다르기 때문**입니다.
심의 목록은 학교의 공식 자산 목록이고, 신청은 그 목록에 들어가기 전의 요청입니다.
한 테이블에 상태 컬럼으로 합치면 "교사가 올린 미확인 정보"와 "학교가 인정한 정보"가
같은 표에 섞여, 조회 화면에서 매번 걸러 내야 합니다.

### 5.2 테이블 명세

**approved_software — 심의받은 소프트웨어 목록** (신규)

| 컬럼                 | 형식        | 필수 | 설명                                                    |
| -------------------- | ----------- | ---- | -------------------------------------------------------- |
| id                   | uuid        | ○    | 기본키                                                   |
| school_id            | uuid        | ○    | `school_config(id)` 참조, on delete cascade              |
| name                 | text        | ○    | 소프트웨어명                                             |
| company              | text        |      | 회사(제작사)                                             |
| eduzip_registered    | boolean     | ○    | 에듀집 등록 여부, 기본 `false`                           |
| eduzip_url           | text        |      | 주소(URL)                                                |
| note                 | text        |      | 비고                                                     |
| is_active            | boolean     | ○    | 기본 `true`. `false`면 목록에서 숨김 (S-04)              |
| source_request_id    | uuid        |      | 신청에서 편입된 경우 `software_requests(id)`, on delete set null |
| created_at           | timestamptz | ○    |                                                          |
| updated_at           | timestamptz | ○    |                                                          |

**software_requests — 교사가 신청한 소프트웨어** (신규)

| 컬럼                 | 형식        | 필수 | 설명                                                     |
| -------------------- | ----------- | ---- | --------------------------------------------------------- |
| id                   | uuid        | ○    | 기본키                                                    |
| school_id            | uuid        | ○    | `school_config(id)` 참조, on delete cascade               |
| classroom_id         | uuid        |      | `classrooms(id)` 참조, on delete set null (학급이 지워져도 신청 이력은 남김) |
| requester_name       | text        | ○    | 신청 교사명                                               |
| name                 | text        | ○    | 소프트웨어명                                              |
| company              | text        | ○    | 회사(제작사)                                              |
| eduzip_registered    | boolean     | ○    | 교사가 표시한 에듀집 등록 여부, 기본 `false`              |
| eduzip_url           | text        | ○    | 주소(URL)                                                 |
| purpose              | text        |      | 사용 목적                                                 |
| status               | text        | ○    | `'처리중'` / `'심의완료'`, 기본 `'처리중'` (check 제약)   |
| processed_at         | timestamptz |      | 상태를 마지막으로 바꾼 시각                               |
| processed_by         | text        |      | 처리한 관리자 이메일 (계정이 사라져도 남도록 문자열 저장) |
| approved_software_id | uuid        |      | 심의완료 시 편입된 `approved_software(id)`, on delete set null |
| created_at           | timestamptz | ○    |                                                           |
| updated_at           | timestamptz | ○    |                                                           |

`processed_by`를 FK가 아닌 문자열로 두는 것은 기존 `teacher_device_loans`의
`manager_out_name` 처리 방식과 같은 이유입니다. 담당자가 전근을 가서 계정이 사라져도
누가 처리했는지는 기록에 남아야 합니다.

### 5.3 중복 판정 규칙

엑셀 업로드(S-01)와 신청 편입(S-12)에서 "이미 있는 항목"을 판정하는 기준입니다.

```
정규화 키 = lower(trim(name)) + '|' + lower(trim(coalesce(company, '')))
```

- 공백과 대소문자만 다른 항목은 같은 것으로 봅니다.
- 회사명이 비어 있으면 이름만으로 비교합니다.
- 이 키로 `unique index`를 걸어 **DB 차원에서도 중복을 막습니다.**
- 같은 이름이라도 회사가 다르면 별개 항목입니다 (예: 여러 회사의 동명 학습앱).

### 5.4 상태 전이

```
        [ 교사 신청 ]
              │
              ▼
         ┌─────────┐   관리자 변경    ┌───────────┐
         │ 처리중  │ ───────────────▶ │ 심의완료  │
         └─────────┘ ◀─────────────── └───────────┘
                        관리자 되돌림
```

- 상태를 바꿀 수 있는 사람은 **관리자뿐**입니다.
- `심의완료` → `처리중`으로 되돌리는 것도 허용합니다(오조작 정정).
  다만 **되돌려도 이미 편입된 심의 목록 항목은 자동으로 지우지 않습니다.**
  목록에서 빼려면 관리자가 해당 항목을 `사용중지`(S-04) 처리합니다.
  자동 삭제하면 교사들이 이미 쓰고 있던 항목이 예고 없이 사라지기 때문입니다.
- 삭제는 어느 쪽에도 두지 않습니다. 기존 탭들과 동일하게 상태 변경·사용중지로만 다룹니다.

### 5.5 운영자 페이지 반영

기존 `school_tab_activity` 뷰의 `UNION ALL`에 이 탭의 활동을 추가합니다.

```sql
union all
select t.school_id, '소프트웨어'::text as tab, t.created_at as at from approved_software t
union all
select t.school_id, '소프트웨어'::text as tab, t.created_at as at from software_requests t
```

이렇게 해야 운영자 대시보드의 탭 사용 현황에서 새 탭이 `미사용`/`저조`/`활발`로
집계됩니다. 인덱스도 기존 규칙대로 `(school_id, created_at)`에 추가합니다.

---

## 6. 권한과 보안

### 6.1 학교별 분리

기존 플랫폼과 동일하게 **애플리케이션 계층에서 학교를 분리**합니다.
교사 로그인이 Supabase Auth 세션 없이 anon 키로 동작하기 때문에, 이 탭만 별도로
RLS를 강화할 수 없습니다.

```
관리자 → admin_profiles에서 school_id 조회 (useSchool 훅)
교사   → localStorage의 teacher_session.school_id 사용
       → 모든 select / insert / update 쿼리에 .eq('school_id', schoolId) 적용
       → RLS 정책은 기존과 동일하게 for all using (true)
```

**한계**: Supabase URL과 anon 키를 직접 다루는 사용자가 API를 직접 호출하면 이론상
다른 학교 데이터에 접근할 수 있습니다. 이는 이 기능만의 문제가 아니라 플랫폼 전체에
이미 존재하는 한계입니다. 다만 이 탭이 다루는 데이터는 **학교가 심의해 공개하기로 한
소프트웨어 목록**이라 민감도가 상대적으로 낮습니다(신청 교사명은 예외 — 6.3 참고).

### 6.2 권한 정책

| 대상               | 조회                              | 입력           | 수정                     | 삭제     |
| ------------------ | --------------------------------- | -------------- | ------------------------ | -------- |
| 심의 목록          | 같은 학교의 교사·관리자           | 관리자         | 관리자                   | **불가** (사용중지로 대체) |
| 신청 기록          | 관리자 전체 / 교사는 같은 학교 범위 | 교사·관리자   | 상태 변경은 관리자만     | **불가** |

교사 화면에서는 상태 변경 UI 자체를 그리지 않고, 저장 쿼리에도 `status` 필드를
포함시키지 않습니다. 화면을 숨기는 것만으로는 부족하므로 **교사 화면의 update 쿼리는
신청 본문 수정 자체를 지원하지 않습니다**(잘못 신청한 건은 관리자에게 말해 처리).

### 6.3 개인정보·입력값 취급

- **신청 교사명**이 남습니다. 같은 학교 교사 사이에서만 보이도록 조회 범위를 학교로
  한정하고, 그 이상의 활용(통계·평가)은 하지 않습니다.
- **URL 입력** — 저장 전에 `http://` 또는 `https://`로 시작하는지 확인하고, 없으면
  `https://`를 붙입니다. `javascript:` 등 다른 스킴은 저장하지 않고 안내를 띄웁니다.
  링크에는 `rel="noopener noreferrer"`를 붙입니다.
- **길이 제한** — 이름·회사 100자, URL 500자, 목적·비고 1000자로 화면에서 제한합니다.
- **엑셀 파일**은 브라우저에서만 파싱하고 서버에 올리지 않습니다(기존 방식과 동일).

---

## 7. 엑셀 업로드 규격 (S-01)

### 7.1 열 제목 매칭

학교마다 엑셀 열 제목이 다르므로, `크롬북 관리` 탭의 `findCol()` 방식으로
**여러 후보 이름을 받아들입니다.** 열 순서는 상관없고, 모르는 열은 무시합니다.

| 항목             | 인식하는 열 제목                                                    |
| ---------------- | -------------------------------------------------------------------- |
| 소프트웨어명     | `소프트웨어명`, `소프트웨어`, `프로그램명`, `이름`, `명칭`, `name`  |
| 회사             | `회사`, `회사명`, `제작사`, `개발사`, `업체`, `공급업체`, `company` |
| 에듀집 등록 여부 | `에듀집`, `에듀집등록`, `에듀집 등록여부`, `등록여부`, `eduzip`     |
| 주소             | `주소`, `URL`, `url`, `링크`, `사이트`, `홈페이지`                  |
| 비고             | `비고`, `메모`, `특이사항`, `note`                                  |

### 7.2 값 해석

| 항목             | 규칙                                                                       |
| ---------------- | --------------------------------------------------------------------------- |
| 에듀집 등록 여부 | `O` `o` `Y` `y` `예` `등록` `TRUE` `1` `√` `✓` → **등록**, 그 외·빈칸 → **미등록** |
| 주소             | 앞뒤 공백 제거. 스킴이 없으면 `https://` 보정                              |
| 이름 없는 행     | 건너뛰고, 미리보기에 "제외된 행 N개"로 안내                                |
| 파일 내 중복     | 5.3 정규화 키 기준으로 **뒤 행이 앞 행을 덮어씀**                          |

### 7.3 등록 동작

1. 파일을 읽어 정규화합니다 (`xlsx`를 동적 import — 기존과 동일).
2. 현재 학교의 심의 목록을 불러와 5.3 키로 신규/갱신을 판정합니다.
3. 미리보기 표를 보여 줍니다. 이 시점까지 DB에 아무것도 쓰지 않습니다.
4. `확인`을 누르면 신규는 `insert`, 갱신은 `update`로 나눠 보냅니다.
   - 갱신 시 **엑셀에 빈칸인 열은 기존 값을 덮어쓰지 않습니다.**
     부분 목록을 올렸다가 기존 정보가 지워지는 사고를 막기 위해서입니다.
   - 갱신 대상이 `is_active = false`였다면 다시 `true`로 되살립니다.
5. 결과를 `audit_logs`에 한 건으로 요약해 남깁니다
   (예: `심의 소프트웨어 엑셀 등록 — 신규 18건, 갱신 6건`).

### 7.4 엑셀 양식 (S-16)

`양식 다운로드`는 아래 한 줄짜리 시트를 내려줍니다.

| 소프트웨어명 | 회사 | 에듀집 등록여부 | 주소 | 비고 |
| ------------ | ---- | --------------- | ---- | ---- |

---

## 8. 기술 구성

기존 플랫폼과 동일한 스택입니다. 새 의존성을 추가하지 않습니다.

| 영역         | 선택                                   | 이유                                                        |
| ------------ | --------------------------------------- | ------------------------------------------------------------ |
| 프론트엔드   | Next.js 16 (App Router), React 19       | 기존 대시보드에 라우트 두 개만 추가                         |
| 스타일       | Tailwind CSS v4                         | 기존 `Button`·`Modal`·`Badge` 재사용                        |
| 데이터베이스 | Supabase (PostgreSQL)                   | 기존 `school_config`·`classrooms`에 신규 테이블 연결        |
| 엑셀 처리    | `xlsx` (이미 설치됨, 동적 import)       | `크롬북 관리` 탭과 동일한 코드 패턴                         |
| 인증         | 관리자 = Supabase Auth / 교사 = 학급 세션 | 기존 로그인 그대로                                        |
| 감사 로그    | `logAudit()` / `diffFields()`           | 기존 유틸 그대로 사용                                       |
| 배포         | Vercel                                  | 기존 파이프라인 그대로                                      |

**타입 추가** (`src/types/index.ts`)

```ts
export type SoftwareRequestStatus = '처리중' | '심의완료'

export interface ApprovedSoftware { … }
export interface SoftwareRequest { …, classrooms?: Classroom }
```

---

## 9. 개발 단계

| 단계 | 내용                                                        | 산출물                                  |
| ---- | ------------------------------------------------------------ | ---------------------------------------- |
| 1    | `approved_software`·`software_requests` 테이블 및 마이그레이션 SQL | `supabase-schema.sql` 갱신          |
| 2    | 타입 정의, 양쪽 사이드바에 `소프트웨어 조회` 탭 추가        | `types/index.ts`, 사이드바 2개          |
| 3    | 관리자 심의 목록 화면 — 조회·개별 추가·수정·사용중지 (S-02~05) | `/admin/software`                     |
| 4    | 엑셀 일괄 등록 — 파싱·미리보기·확정 (S-01)                  | 업로드 모달                              |
| 5    | 검색·색인·필터·정렬 공통 부품 (S-06~08)                     | 목록 부품 (양쪽 화면에서 공유)          |
| 6    | 교사 심의 목록 화면 (S-05~08 재사용)                        | `/teacher/software`                     |
| 7    | 교사 신청 화면·신청 모달·중복 안내 (S-09, S-10, S-13)       | 신청 모달                                |
| 8    | 관리자 신청 현황·상태 변경·목록 편입 (S-11, S-12)           | 상태 변경 모달                           |
| 9    | 감사 로그 연결, 운영자 뷰 갱신 (S-15, 5.5)                  | `audit.ts` 호출, 뷰 SQL                  |
| 10   | 검수·배포                                                    | 실제 엑셀 파일로 확인 후 배포           |

선택 기능(S-16·S-17)은 5~8단계와 함께 여력이 되면 넣고, 아니면 1차 배포 후 판단합니다.

---

## 10. 검수 기준

배포 전 아래를 실제로 해 보고 확인합니다.

**심의 목록**

- [ ] 실제 학교 심의 엑셀 파일을 올려 열 제목이 자동으로 인식된다
- [ ] 미리보기에서 신규·갱신 구분이 맞다
- [ ] 미리보기 단계에서 `취소`하면 DB에 아무것도 남지 않는다
- [ ] 같은 파일을 두 번 올려도 항목이 두 배로 늘지 않는다
- [ ] 일부 열만 채운 파일로 갱신해도 기존 값이 지워지지 않는다
- [ ] 에듀집 열에 `O`/`X`, `Y`/`N`, `예`/`아니오`를 넣어도 모두 인식된다
- [ ] 개별 추가로 넣은 항목이 목록 맨 위(최근 등록순)에 보인다
- [ ] 사용중지한 항목이 교사 화면에서 사라진다

**검색·색인**

- [ ] `ㅁ`을 누르면 ㅁ으로 시작하는 항목만 남는다
- [ ] 쌍자음으로 시작하는 이름(예: `또바기`)이 `ㄷ` 색인에 들어간다
- [ ] 영문 이름이 해당 알파벳 색인에, 숫자로 시작하는 이름이 `#`에 들어간다
- [ ] 항목이 없는 색인 글자는 눌리지 않는다
- [ ] 검색어와 색인을 함께 걸면 둘 다 만족하는 항목만 남는다
- [ ] 회사명으로도 검색된다

**신청**

- [ ] 교사가 신청하면 `처리중` 상태로 즉시 목록에 나타난다
- [ ] 필수 항목(이름·회사·URL·신청 교사명)이 비면 저장되지 않고 안내가 뜬다
- [ ] `www.example.com`처럼 스킴 없이 넣어도 링크가 정상 동작한다
- [ ] 이미 심의 목록에 있는 이름을 입력하면 신청 전에 안내가 보인다
- [ ] 관리자가 `심의완료`로 바꾸면 교사 화면의 배지도 바뀐다
- [ ] `심의 목록에도 추가`를 켜고 처리하면 심의 목록에 나타난다
- [ ] 체크를 끄고 처리하면 상태만 바뀌고 목록에는 추가되지 않는다
- [ ] 교사 화면에는 상태를 바꾸는 버튼이 없다

**분리**

- [ ] 다른 학교 관리자로 로그인하면 우리 학교 목록이 보이지 않는다
- [ ] 교사 계정에서 보이는 목록이 그 학교의 것과 일치한다

**기록**

- [ ] 엑셀 등록·개별 추가·수정·상태 변경이 `감사 로그` 탭에 남는다
- [ ] 운영자 대시보드 탭 사용 현황에 `소프트웨어`가 집계된다

---

## 11. 향후 과제

| 과제              | 내용                                                              | 시점            |
| ----------------- | ------------------------------------------------------------------ | ---------------- |
| 반려 상태·사유    | `반려`를 추가하고 관리자가 사유를 남겨 교사에게 보여 줌 (S-18)    | 운영 후 필요 시 |
| 교사 알림         | 심의완료 시 교사 대시보드 알림 (S-19)                             | 2차             |
| 사용 학년·과목 태그 | 목록에 학년·과목 태그를 달아 필터                                | 2차             |
| 학교 간 공유      | 다른 학교의 심의 결과를 참고 목록으로 열람 (운영자 기능과 연계)   | 검토 필요       |
| 개인정보 항목     | 소프트웨어별 수집 개인정보 항목·처리방침 링크 보관                | 검토 필요       |

---

## 12. 검토가 필요한 사항

| 사항                | 내용                                                                                            |
| ------------------- | ------------------------------------------------------------------------------------------------ |
| `에듀집` 정식 명칭  | 화면 라벨·엑셀 열 제목에 쓸 정확한 명칭을 확인해 주세요. 등록번호처럼 함께 남겨야 할 값이 있다면 컬럼을 하나 더 둡니다 |
| 심의 주체           | 학교 자체 심의인지, 교육청 심의 결과를 옮겨 적는 것인지에 따라 목록의 갱신 주기와 책임 범위가 달라집니다 |
| 신청 교사 식별      | 지금은 학급 계정 + 이름 직접 입력입니다. 교사 개인 계정이 필요해지면 로그인 구조 전체를 함께 검토해야 합니다 |
| 반려 처리           | 1차 상태는 `처리중`·`심의완료` 둘뿐입니다. 심의에서 떨어진 건을 어떻게 표시할지(계속 `처리중`으로 둘지, `반려`를 넣을지) 정해 주세요 |
| 목록 공개 범위      | 심의 목록을 학생·학부모에게도 보여 줄 필요가 있는지(로그인 없는 공개 페이지)                     |
| 초기 데이터         | 기존 심의 목록 엑셀 파일 한 개를 미리 받아 열 제목 매칭 규칙(7.1)을 실제 파일 기준으로 확정하는 편이 좋습니다 |

---

## 부록 A. 스키마 SQL 초안

`app/supabase-schema.sql` 하단의 기존 규칙(테이블 → RLS → 인덱스)을 따릅니다.

```sql
-- 15. 심의받은 소프트웨어 목록
create table if not exists approved_software (
  id uuid primary key default gen_random_uuid(),
  school_id uuid not null references school_config(id) on delete cascade,
  name text not null,
  company text,
  eduzip_registered boolean not null default false,
  eduzip_url text,

  note text,
  is_active boolean not null default true,
  source_request_id uuid,
  created_at timestamptz default now(),
  updated_at timestamptz default now()
);

-- 학교 안에서 이름+회사 중복 방지 (공백·대소문자 무시)
create unique index if not exists idx_approved_software_unique
  on approved_software (school_id, lower(btrim(name)), lower(btrim(coalesce(company, ''))));

-- 16. 교사가 신청한 소프트웨어
create table if not exists software_requests (
  id uuid primary key default gen_random_uuid(),
  school_id uuid not null references school_config(id) on delete cascade,
  classroom_id uuid references classrooms(id) on delete set null,
  requester_name text not null,
  name text not null,
  company text not null,
  eduzip_registered boolean not null default false,
  eduzip_url text not null,
  purpose text,
  status text not null default '처리중'
    check (status in ('처리중', '심의완료')),
  processed_at timestamptz,
  processed_by text,
  approved_software_id uuid references approved_software(id) on delete set null,
  created_at timestamptz default now(),
  updated_at timestamptz default now()
);

-- approved_software.source_request_id 는 순환 참조를 피하기 위해 테이블 생성 후 FK 추가
alter table approved_software
  drop constraint if exists approved_software_source_request_fk;
alter table approved_software
  add constraint approved_software_source_request_fk
  foreign key (source_request_id) references software_requests(id) on delete set null;

-- RLS (기존 테이블과 동일한 전체 허용 정책)
alter table approved_software enable row level security;
alter table software_requests enable row level security;

drop policy if exists "approved_software_all" on approved_software;
drop policy if exists "software_requests_all" on software_requests;

create policy "approved_software_all" on approved_software for all using (true) with check (true);
create policy "software_requests_all" on software_requests for all using (true) with check (true);

-- 인덱스
create index if not exists idx_approved_software_school_created
  on approved_software (school_id, created_at);
create index if not exists idx_software_requests_school_created
  on software_requests (school_id, created_at);
create index if not exists idx_software_requests_school_status
  on software_requests (school_id, status);
```

운영자 뷰(`school_tab_activity`)는 5.5의 `union all` 두 줄을 추가해 다시 만듭니다.

## 부록 B. 화면·경로 요약

| 구분   | 경로                | 안쪽 탭                    | 주요 동작                                      |
| ------ | ------------------- | -------------------------- | ----------------------------------------------- |
| 관리자 | `/admin/software`   | 심의 목록 / 신청 현황      | 엑셀 등록, 개별 추가, 수정, 사용중지, 상태 변경 |
| 교사   | `/teacher/software` | 심의 목록 / 내 신청        | 검색·색인 조회, 신청                            |
